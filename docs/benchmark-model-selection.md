# Benchmark model-selection notes

This is a planning note, not a frozen campaign registry or permission to dispatch API calls.

## Required model

On 2026-09-05, the user requested that forthcoming API benchmark runs include GPT-6.
Its exact API model ID, availability to the benchmark account, settings, and pricing have not
yet been verified. Do not silently substitute another model if GPT-6 is unavailable.

## Before a live run

- Confirm the exact available GPT-6 API ID and discuss the remaining model choices with the user.
- Agree on the campaign settings and spending limit, then freeze the model registry through the
  existing approved workflow.
- Preserve the preregistered campaign shape unless the user explicitly approves a change.

This note does not change existing protocol hashes, select a fourth model, or start a paid run.
